# Implement the server and Remote Control transport in TypeScript

Implement the app-server daemon, Pi adapter, and OpenAI Remote Control transport in TypeScript rather than shipping a Rust compatibility sidecar. Use the pinned official Codex source as the behavioral reference and port its protocol details, tests, retry rules, limits, and security checks where they apply.
