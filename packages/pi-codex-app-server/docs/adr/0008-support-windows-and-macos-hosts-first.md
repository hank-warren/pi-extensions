# Support Windows and macOS connected hosts first

Release Connected host support for Windows x64 first, followed by macOS arm64 and x64. Treat Linux x64 and arm64 as experimental because ChatGPT Remote documents Windows and macOS hosts, even where the transport implementation itself is portable.
