<!--
Thanks for the pull request. A few notes before you submit:

- Open it from a fork against `main`.
- Do NOT add a changeset. Releases are cut separately, after the change has been
  exercised in a real session. See AGENTS.md.
- Conventional commit style, lowercase, no emojis.
-->

## What this changes

<!-- A sentence or two. If it fixes an issue, link it: "Fixes #12". -->

## Why

<!-- The problem behind the change. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run scan-secrets` passes
- [ ] No changeset added (releases are cut separately)
- [ ] No cross-package relative imports
- [ ] Package README updated, if behavior changed
