# Every change from anyone but the repository owner requires the owner's review.
# Paired with classic branch protection (require code-owner review, required
# approvals 0 — see AGENTS.md "Branch protection"), this keeps `main`
# maintainer-only while still letting anyone open a pull request from a fork.

* @hank-warren
